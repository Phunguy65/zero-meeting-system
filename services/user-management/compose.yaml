services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=zms_users'
      - 'POSTGRES_PASSWORD=zms_secret'
      - 'POSTGRES_USER=zms_user'
    ports:
      - '5432:5432'
